
name: Create Master Update ZIP

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  package:
    runs-on: windows-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Create master_update_current.zip
        shell: powershell
        run: |
          Compress-Archive `
            -Path "manifest.json","masters","templates","profiles" `
            -DestinationPath "master_update_current.zip" `
            -Force

      - name: Upload ZIP artifact
        uses: actions/upload-artifact@v4
        with:
          name: master_update_current
          path: master_update_current.zip
